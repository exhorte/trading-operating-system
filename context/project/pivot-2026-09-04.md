# Journal du pivot — 2026-09-04

Document unique, non destiné à être mis à jour. Il existe pour qu'une session future comprenne pourquoi le dépôt a changé de forme en un jour.

## Décisions prises par l'utilisateur ce jour

1. **La recherche d'edge est abandonnée définitivement.** Le holdout vierge ne sera jamais lu ; la Phase 13 ne sera jamais close.
2. **L'historique de recherche est supprimé du dossier de travail** — il reste dans l'historique git.
3. **Le dépôt GitHub est nettoyé par commits**, sans réécriture d'historique.
4. **La gouvernance par phases est remplacée** par une gouvernance par outils et vagues.

## Ce qui a été supprimé

Déplacé vers `../_to_delete/2026-09-04-pivot/` (hors dépôt) puis retiré du suivi git.

**Code** — `lib/backtest/` (12 fichiers), `lib/strategy/` (4), `scripts/backtest.ts`, `scripts/backtest-report.ts`, `scripts/calibrate-spread.ts`, `query_ticks.csx`, `app/(cockpit)/backtests/`, `components/cockpit/backtests-workspace.tsx`, `backend/src/TradingOs.Persistence/BacktestRepository.cs`, `tools/mt5-observer/inspect_symbol.py`, `tools/mt5-observer/swap_capture_XAUUSDm_2026-07-27.json`.

**Modifié** — `Program.cs` (registration + endpoints `/api/backtests` retirés), `schema.sql` (tables `backtest_*` et `holdout_*` retirées), `components/shell/nav.ts` (entrée Backtests retirée).

**Documentation** — `context/project/phases/` (24 fiches), `context/adr/` (série 0001–0013), `context/backtesting/`, `context/agents/`, `context/prompts/`, `context/domain/ea_analysis.md`, `context/knowledge/source_notes_index.md`, `context/templates/phase-template.md`, `context/project/{changelog,session-history,handoff,memory,project_state,roadmap,development_manifesto}.md`, `.claude/commands/phase-{start,close}.md`, `.claude/prompts/`, `search.md`.

## Ce qui a été conservé, et pourquoi

- `lib/analysis/` — les niveaux, sessions et ATR alimentent le brief pré-séance (T10) et les captures (T05). Le moteur reste une **source de contexte**, plus jamais une source de signal.
- `lib/risk/` — c'est le socle de T01, T02 et T09.
- Toute la chaîne temps réel et la persistance — validées en live, coûteuses à reconstruire, indispensables à tout le reste.
- `context/realtime/`, `architecture/`, `backend/`, `engineering/`, `domain/`, `frontend/`, `security/`, `monitoring/`, `infrastructure/` — spécifications techniques toujours exactes, en anglais, laissées telles quelles.

## Ce qui n'a pas pu être vérifié dans cette session

Le poste de travail où tourne cette session ne dispose ni de `dotnet`, ni de `docker`, ni des dépendances npm installées. **Les gates n'ont pas été exécutées.** La suppression a été validée statiquement : aucune référence orpheline vers un module supprimé ne subsiste dans `app/`, `components/`, `lib/`, `scripts/` ou `backend/`.

La première action de la prochaine session est la séquence de vérification de `context/infrastructure/runbook.md`.

## Incident à signaler

Pendant cette session, `context/` et `.claude/` ont disparu du répertoire de travail — 101 fichiers — sans qu'aucune commande de suppression ait été lancée (la suppression est refusée par défaut sur ce montage, et le reflog ne contient que le clone initial). Les fichiers ont été restaurés depuis `HEAD` sans perte. Cause non identifiée. Si le phénomène se reproduit, suspecter un antivirus, un outil de synchronisation ou un autre agent tournant sur la machine.
